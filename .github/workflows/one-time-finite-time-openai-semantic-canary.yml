name: FINITE TIME OpenAI Semantic Canary

on:
  push:
    branches: [main]
    paths:
      - authorizations/execute-finite-time-openai-semantic-canary-20260912.json

permissions:
  contents: read

concurrency:
  group: finite-time-openai-semantic-canary-20260912
  cancel-in-progress: false

jobs:
  generate:
    if: github.run_attempt == 1
    runs-on: ubuntu-latest
    timeout-minutes: 60
    environment: paid-asset-generation
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        shot: [8, 28]
    steps:
      - name: Checkout exact authorization commit
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          ref: ${{ github.sha }}
          fetch-depth: 2
          persist-credentials: false

      - name: Prove exact defect-canary authority
        shell: bash
        run: |
          set -euo pipefail
          test "$GITHUB_EVENT_NAME" = push
          test "$GITHUB_REF" = refs/heads/main
          test "$GITHUB_RUN_ATTEMPT" = '1'
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          parents="$(git rev-list --parents -n 1 "$GITHUB_SHA")"
          test "$(wc -w <<<"$parents")" -eq 3
          first_parent="$(git rev-parse "$GITHUB_SHA^1")"
          marker='authorizations/execute-finite-time-openai-semantic-canary-20260912.json'
          workflow='.github/workflows/one-time-finite-time-openai-semantic-canary.yml'
          adapter='scripts/check-paid-workflow-boundary-current.py'
          fixture='scripts/test-paid-workflow-current-authorities.py'
          changed="$(git diff --name-only "$first_parent" "$GITHUB_SHA" | sort)"
          expected="$(printf '%s\n%s\n%s\n%s\n' "$workflow" "$marker" "$adapter" "$fixture" | sort)"
          test "$changed" = "$expected"
          test -z "${OPENAI_API_KEY:-}"
          python - <<'PY'
          import json
          m=json.load(open('authorizations/execute-finite-time-openai-semantic-canary-20260912.json'))
          assert m['schemaVersion']=='finite-time-nonprivate-semantic-canary-v1'
          assert m['confirmation']=='AUTHORIZE_FINITE_TIME_NONPRIVATE_SEMANTIC_CANARY'
          assert m['productionSource']=='32041da2e66d553ce8e2aa696bb125cc25261ac5'
          assert m['defectSourceRun']==34677073895
          assert m['provider']=='openai'
          assert m['model']=='sora-2'
          assert m['shotNumbers']==[8,28]
          assert m['maximumProviderCalls']==2
          assert m['estimatedMaximumSpendUsd']==1.60
          assert m['automaticRetryAuthorized'] is False
          assert m['privateProductionOnly'] is True
          assert m['containsPrivateReferenceMedia'] is False
          assert m['promotionAuthorized'] is False
          assert m['deploymentAuthorized'] is False
          assert m['deliveryAuthorized'] is False
          assert m['publicReleaseAuthorized'] is False
          assert m['literalVisualQcRequired'] is True
          assert m['successDoesNotAuthorizeRemainingRetries'] is True
          PY

      - name: Checkout repaired governed production source
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          ref: 32041da2e66d553ce8e2aa696bb125cc25261ac5
          path: production-src
          fetch-depth: 1
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.11'

      - name: Generate one semantic canary shot
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          FINITE_TIME_VIDEO_MODEL: sora-2
          FINITE_TIME_VIDEO_SIZE: 1280x720
          SHOT_NUMBER: ${{ matrix.shot }}
        shell: bash
        run: |
          set -euo pipefail
          test -n "$OPENAI_API_KEY"
          root="$GITHUB_WORKSPACE/production-src"
          out="$GITHUB_WORKSPACE/artifacts/finite-time/semantic-canary/shot-${SHOT_NUMBER}"
          python "$root/film_foundry/finite_time/generate-openai-production.py" \
            --manifest "$root/film_foundry/finite_time/farm-to-lake.manifest.json" \
            --edit-plan "$root/film_foundry/finite_time/farm-to-lake.edit-plan.v2.json" \
            --authorization "$root/film_foundry/finite_time/openai-production-authorization.v2.json" \
            --output-root "$out" \
            --start-shot "$SHOT_NUMBER" --end-shot "$SHOT_NUMBER"
          python - "$out/provider-receipt.json" <<'PY'
          import json,sys
          r=json.load(open(sys.argv[1]))
          assert r['status']=='generated-awaiting-literal-qc', r
          assert r['providerCallsExecuted']==1, r
          assert len(r['videos'])==1, r
          assert r['videos'][0]['referenceUsed'] is False, r
          assert r['videos'][0]['acceptanceStatus']=='awaiting-literal-visual-qc', r
          assert r['publicReleaseAuthorized'] is False, r
          PY

      - name: Upload semantic canary evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: finite-time-semantic-canary-shot-${{ matrix.shot }}-${{ github.run_id }}
          retention-days: 30
          compression-level: 0
          if-no-files-found: warn
          path: artifacts/finite-time/semantic-canary/shot-${{ matrix.shot }}/**
