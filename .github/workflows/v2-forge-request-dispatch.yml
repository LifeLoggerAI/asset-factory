name: V2 Forge Request Dispatch (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Redundant dispatcher disabled; canonical V2 forge owns request handling."
