name: Asset Factory Engine Security Lock Refresh

on:
  push:
    branches:
      - hardening/asset-factory-production-authority-20260924
    paths:
      - '.github/workflows/asset-factory-engine-security-lock-refresh.yml'

permissions:
  contents: write

jobs:
  refresh-locks:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: hardening/asset-factory-production-authority-20260924
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: corepack enable && corepack prepare pnpm@9.15.9 --activate
      - name: Refresh workspace and engine lock authority
        shell: bash
        run: |
          set -euo pipefail
          pnpm install --lockfile-only
          npm --prefix engine install --package-lock-only --ignore-scripts
      - name: Commit lock updates only when changed
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add pnpm-lock.yaml engine/npm-shrinkwrap.json
          if git diff --cached --quiet; then
            echo "Locks already current"
            exit 0
          fi
          git commit -m "chore(lock): refresh patched Asset Factory engine locks"
          git push origin HEAD:hardening/asset-factory-production-authority-20260924
