name: V1 AAA Asset Forge

on:
  workflow_dispatch:
    inputs:
      rounds:
        description: Maximum provider generation and upgrade rounds
        required: true
        default: "3"
        type: string
      job_id:
        description: Optional URAI Jobs job ID
        required: false
        default: "manual"
        type: string
      callback_url:
        description: Optional URAI Jobs callback URL
        required: false
        default: ""
        type: string
  repository_dispatch:
    types: [urai-v1-forge-requested]
  push:
    branches: [main]
    paths:
      - .github/asset-forge-trigger/v1.txt

concurrency:
  group: urai-v1-aaa-asset-forge
  cancel-in-progress: false

permissions:
  contents: read
  actions: read

jobs:
  forge-and-promote:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    outputs:
      spatial_sha: ${{ steps.promote.outputs.spatial_sha }}
      job_id: ${{ steps.context.outputs.job_id }}
      can_promote: ${{ steps.promotion.outputs.can_promote }}
    env:
      ASSET_RENDERER_ENDPOINT_SECRET: ${{ secrets.ASSET_RENDERER_ENDPOINT }}
      ASSET_RENDERER_API_KEY_SECRET: ${{ secrets.ASSET_RENDERER_API_KEY }}
      OPENAI_API_KEY_SECRET: ${{ secrets.OPENAI_API_KEY }}
      ASSET_RENDERER_MODEL: ${{ secrets.ASSET_RENDERER_MODEL }}
      ASSET_RENDERER_ALPHA_MODEL: gpt-image-1.5
      ASSET_RENDERER_AUTH_HEADER: ${{ secrets.ASSET_RENDERER_AUTH_HEADER }}
      ASSET_RENDERER_AUTH_SCHEME: ${{ secrets.ASSET_RENDERER_AUTH_SCHEME }}
      URAI_WHEEL_GITHUB_TOKEN: ${{ secrets.URAI_WHEEL_GITHUB_TOKEN }}
    steps:
      - name: Resolve request context
        id: context
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "repository_dispatch" ]; then
            echo "rounds=${{ github.event.client_payload.rounds || '3' }}" >> "$GITHUB_OUTPUT"
            echo "job_id=${{ github.event.client_payload.job_id || 'dispatch' }}" >> "$GITHUB_OUTPUT"
            echo "callback_url=${{ github.event.client_payload.callback_url || '' }}" >> "$GITHUB_OUTPUT"
          elif [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "rounds=${{ inputs.rounds }}" >> "$GITHUB_OUTPUT"
            echo "job_id=${{ inputs.job_id }}" >> "$GITHUB_OUTPUT"
            echo "callback_url=${{ inputs.callback_url }}" >> "$GITHUB_OUTPUT"
          else
            echo "rounds=3" >> "$GITHUB_OUTPUT"
            echo "job_id=controlled-push" >> "$GITHUB_OUTPUT"
            echo "callback_url=" >> "$GITHUB_OUTPUT"
          fi

      - name: Checkout Asset Factory
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install image dependencies
        run: python -m pip install -r image_asset_generator/requirements.txt

      - name: Resolve production renderer configuration
        shell: bash
        run: |
          endpoint="$ASSET_RENDERER_ENDPOINT_SECRET"
          api_key="$ASSET_RENDERER_API_KEY_SECRET"
          [ -n "$api_key" ] || api_key="$OPENAI_API_KEY_SECRET"

          if [ -n "$endpoint" ]; then
            echo "ASSET_RENDERER_ENDPOINT=$endpoint" >> "$GITHUB_ENV"
            case "$endpoint" in
              *api.openai.com*)
                provider="openai"
                if [ -z "$api_key" ]; then
                  echo "::error::The OpenAI renderer endpoint is configured but neither ASSET_RENDERER_API_KEY nor OPENAI_API_KEY is present."
                  exit 1
                fi
                ;;
              *)
                provider="custom"
                ;;
            esac
          else
            if [ -z "$api_key" ]; then
              echo "::error::No renderer is configured. Add ASSET_RENDERER_ENDPOINT for the existing custom renderer, or add ASSET_RENDERER_API_KEY/OPENAI_API_KEY for direct OpenAI generation."
              exit 1
            fi
            provider="openai"
            echo "ASSET_RENDERER_ENDPOINT=https://api.openai.com/v1/images/generations" >> "$GITHUB_ENV"
          fi

          echo "ASSET_RENDERER_PROVIDER=$provider" >> "$GITHUB_ENV"
          echo "ASSET_RENDERER_API_KEY=$api_key" >> "$GITHUB_ENV"
          echo "OPENAI_API_KEY=$api_key" >> "$GITHUB_ENV"
          echo "Renderer provider resolved: $provider"
          echo "Renderer endpoint configured: yes"
          if [ -n "$api_key" ]; then
            echo "Renderer API key configured: yes"
          else
            echo "Renderer API key configured: no (custom endpoint mode)"
          fi

      - name: Resolve promotion availability
        id: promotion
        shell: bash
        run: |
          if [ -n "$URAI_WHEEL_GITHUB_TOKEN" ]; then
            echo "can_promote=true" >> "$GITHUB_OUTPUT"
            echo "Cross-repository promotion: enabled"
          else
            echo "can_promote=false" >> "$GITHUB_OUTPUT"
            echo "::warning::URAI_WHEEL_GITHUB_TOKEN is not configured. Generation and evidence upload will continue; automatic promotion to urai-spatial will be skipped."
          fi

      - name: Run feedback-driven V1 forge
        env:
          ASSET_FORGE_MAX_ROUNDS: ${{ steps.context.outputs.rounds }}
          ASSET_RENDERER_MODE: provider
          ASSET_FORGE_REQUIRE_PROVIDER: "1"
          ASSET_QUALITY_REQUIRE_PROVIDER: "1"
        run: python image_asset_generator/forge_v1.py

      - name: Upload forge evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: urai-v1-aaa-forge-${{ github.run_id }}
          if-no-files-found: warn
          retention-days: 30
          path: |
            image_asset_generator/quality_report.json
            image_asset_generator/upgrade_feedback.json
            image_asset_generator/forge_receipt.json
            image_asset_generator/preview.html
            image_asset_generator/asset_pack.zip
            image_asset_generator/spatial_handoff/**

      - name: Checkout Spatial promotion target
        if: steps.promotion.outputs.can_promote == 'true'
        uses: actions/checkout@v4
        with:
          repository: LifeLoggerAI/urai-spatial
          ref: main
          token: ${{ secrets.URAI_WHEEL_GITHUB_TOKEN }}
          path: spatial

      - name: Copy canonical handoff into Spatial
        if: steps.promotion.outputs.can_promote == 'true'
        shell: bash
        run: |
          test -f image_asset_generator/spatial_handoff/assets/urai/final/manifests/asset-factory-spatial-handoff.json
          rsync -a image_asset_generator/spatial_handoff/ spatial/urai-tier1/public/

      - name: Commit and push promoted V1 assets
        if: steps.promotion.outputs.can_promote == 'true'
        id: promote
        working-directory: spatial
        shell: bash
        run: |
          git config user.name "URAI Production Wheel"
          git config user.email "urai-wheel@users.noreply.github.com"
          git add urai-tier1/public/assets/urai
          if git diff --cached --quiet; then
            echo "No asset changes to promote."
          else
            git commit -m "Promote provider-scored V1 AAA asset pack"
            git pull --rebase origin main
            git push origin main
          fi
          echo "spatial_sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Trigger Spatial build deploy and proof
        if: steps.promotion.outputs.can_promote == 'true'
        shell: bash
        run: |
          curl --fail-with-body -sS \
            -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $URAI_WHEEL_GITHUB_TOKEN" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            https://api.github.com/repos/LifeLoggerAI/urai-spatial/dispatches \
            -d '{"event_type":"urai-v1-assets-ready","client_payload":{"asset_factory_run":"${{ github.run_id }}","spatial_sha":"${{ steps.promote.outputs.spatial_sha }}","job_id":"${{ steps.context.outputs.job_id }}"}}'

  notify-jobs:
    if: always()
    needs: [forge-and-promote]
    runs-on: ubuntu-latest
    env:
      CALLBACK_SECRET: ${{ secrets.URAI_JOBS_CALLBACK_SECRET }}
      MANUAL_CALLBACK: ${{ inputs.callback_url }}
      DISPATCH_CALLBACK: ${{ github.event.client_payload.callback_url }}
    steps:
      - name: Notify URAI Jobs when requested
        shell: bash
        run: |
          callback="$MANUAL_CALLBACK"
          [ "${{ github.event_name }}" = "repository_dispatch" ] && callback="$DISPATCH_CALLBACK"
          [ -n "$callback" ] || exit 0
          [ -n "$CALLBACK_SECRET" ] || { echo "::error::URAI_JOBS_CALLBACK_SECRET missing"; exit 1; }
          conclusion="${{ needs.forge-and-promote.result }}"
          status="FAILED"
          [ "$conclusion" = "success" ] && status="SUCCESS"
          curl --fail-with-body -sS \
            -X POST \
            -H "Authorization: Bearer $CALLBACK_SECRET" \
            -H "Content-Type: application/json" \
            "$callback" \
            -d '{"jobId":"${{ needs.forge-and-promote.outputs.job_id }}","status":"'"$status"'","summary":"V1 AAA asset forge '"$conclusion"'","spatialSha":"${{ needs.forge-and-promote.outputs.spatial_sha }}","assetFactoryRun":"${{ github.run_id }}"}'
