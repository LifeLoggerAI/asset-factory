# Next implementation step

Implement canonical `video` support in `assetfactory-studio/lib/server/assetTypeCatalog.ts` and the corresponding renderer, manifest, API, MIME, storage, preview, and E2E coverage while preserving deterministic local-proof mode for CI.
