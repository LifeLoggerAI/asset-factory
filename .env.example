# Root / shared
NODE_ENV=development
PORT=3001

# Stripe (server-side only; leave blank for local non-billing flows)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Firebase Admin (optional for local emulation depending on flow)
GOOGLE_APPLICATION_CREDENTIALS=
