# Provider and budget controls

Canonical counts are V1 53, V2 80, V3 14, V4 39, and V5 27.

Provider requests require explicit authorization plus maximum call, unit-cost, and total-cost values. The ledger is atomic and rejects malformed or tampered state. Provider-required runs fail closed instead of substituting offline output.

No provider call, paid generation, asset promotion, or billing change is claimed by this evidence.
