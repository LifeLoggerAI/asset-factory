# FINITE TIME — Fresh Exact-Head No-Spend Certification

Purpose: trigger the existing `FINITE TIME No-Spend Animatic` pull-request workflow from the current protected production lineage without changing rendering authority or production behavior.

Certification requirements remain unchanged:

- exact checked-out candidate identity;
- deterministic local proof only;
- provider calls executed: 0;
- spend: USD 0;
- secrets unavailable to the render job;
- final rendering unauthorized;
- audited v1 generation and validation;
- storyboard v2 generation and validation;
- retained workflow evidence only after the exact-head job succeeds.

This file grants no provider, spend, merge, deploy, likeness, rights, or final-render authorization.
