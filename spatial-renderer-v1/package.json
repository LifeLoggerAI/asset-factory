{
  "name": "spatial-renderer-v1",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "URAI Spatial scene materialization package for manifests, glTF, GLB placeholders, and USDZ placeholders.",
  "main": "src/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "check": "npm run build"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
